import { QRCodeSVG } from 'qrcode.react';
import { Calendar, MapPin, MoreVertical } from 'lucide-react';
import { demoBooking } from '../data';
import { money, shortDate } from '../utils/format';
export function TicketCard(){const b=demoBooking;return <section className="glass mx-auto max-w-md overflow-hidden rounded-[32px]"><div className="flex gap-4 p-4"><img src={b.movie.poster} className="h-32 w-24 rounded-2xl object-cover"/><div className="flex-1"><div className="flex justify-between"><h2 className="font-bold">{b.movie.title}</h2><MoreVertical size={18}/></div><p className="text-sm text-muted">{b.movie.genre}</p><p className="mt-3 flex items-center gap-2 text-sm"><Calendar size={15} className="text-ember"/>{shortDate(b.show.date)} • {b.show.time}</p><p className="mt-2 flex items-center gap-2 text-sm"><MapPin size={15} className="text-ember"/>{b.cinema.name}</p><p className="mt-2 text-xs text-muted">{b.show.hall} • Row F • Seats {b.seats.map(s=>s.id).join(', ')}</p></div></div><div className="border-y border-dashed border-white/15 p-4"><div className="grid grid-cols-4 gap-3 text-xs"><Info k="Booking ID" v={b.id}/><Info k="Date" v="23.05.26"/><Info k="Total" v={money(b.total)}/><Info k="Payment" v="•••• 4242"/></div></div><div className="flex justify-center p-6"><div className="rounded-2xl bg-white p-3"><QRCodeSVG value={b.qrCode} size={160}/></div></div></section>}
function Info({k,v}:{k:string;v:string}){return <div><p className="uppercase text-muted">{k}</p><p className="mt-1 font-bold">{v}</p></div>}
