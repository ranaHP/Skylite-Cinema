import type { ReactNode } from 'react';
import { motion } from 'framer-motion';
import { ArrowRight, MapPin, Popcorn, Sparkles } from 'lucide-react';
import { Link } from 'react-router-dom';
import { MovieCard } from '../components/MovieCard';
import { cinemas, food, movies } from '../data';
export function HomePage(){return <div className="space-y-12 pt-6"><section className="relative overflow-hidden rounded-[40px] border border-ember/25 bg-black shadow-glow"><img src={movies[0].banner} className="absolute inset-0 h-full w-full object-cover opacity-45"/><div className="absolute inset-0 bg-gradient-to-r from-black via-black/75 to-transparent"/><motion.div initial={{opacity:0,y:30}} animate={{opacity:1,y:0}} className="relative grid min-h-[76vh] items-end gap-8 p-6 md:grid-cols-[1fr_360px] md:p-12"><div className="max-w-3xl"><span className="rounded-full border border-ember/30 bg-ember/10 px-4 py-2 text-sm text-ember">Premium cinematic booking experience</span><h1 className="mt-6 text-4xl font-extrabold leading-tight md:text-6xl">Book the perfect seat for <span className="ember-text">Movie Hub</span> nights.</h1><p className="mt-4 max-w-xl text-muted md:text-lg">Browse showtimes, lock seats in realtime, add gourmet snacks, and receive a premium QR ticket in seconds.</p><div className="mt-8 flex flex-wrap gap-3"><Link to="/booking/dune-2" className="rounded-2xl bg-ember-gradient px-6 py-4 font-semibold shadow-glow">Book Tickets</Link><Link to="/movies/dune-2" className="rounded-2xl border border-ember/30 px-6 py-4 font-semibold text-ember">View Details</Link></div></div><div className="glass rounded-[32px] p-4"><img src={movies[0].poster} className="h-80 w-full rounded-[24px] object-cover"/><div className="mt-4 flex items-center justify-between"><div><p className="text-sm text-muted">Now Showing</p><h3 className="text-2xl font-bold">{movies[0].title}</h3></div><span className="rounded-2xl bg-ember/15 px-3 py-2 text-ember">★ 8.8</span></div></div></motion.div></section><Section title="Featured movies" action="Explore all"><div className="grid grid-cols-2 gap-4 md:grid-cols-4">{movies.map(m=><MovieCard key={m.id} movie={m}/>)}</div></Section><Section title="Cinema locations" action="Find near me"><div className="grid gap-4 md:grid-cols-3">{cinemas.map(c=><div key={c.id} className="glass rounded-[28px] p-5"><MapPin className="text-ember"/><h3 className="mt-3 font-bold">{c.name}</h3><p className="text-sm text-muted">{c.address}</p><p className="mt-3 text-sm text-ember">{c.distance} • ★ {c.rating}</p></div>)}</div></Section><section className="glass grid gap-6 rounded-[36px] p-6 md:grid-cols-2 md:p-8"><div><Popcorn className="text-ember" size={34}/><h2 className="mt-4 text-3xl font-bold">Food & drinks before the credits roll.</h2><p className="mt-3 text-muted">Bundle combos during checkout or add food later from your ticket QR.</p></div><div className="grid gap-3">{food.map(f=><div className="flex items-center gap-3 rounded-3xl bg-white/5 p-3" key={f.id}><img src={f.image} className="h-16 w-16 rounded-2xl object-cover"/><div className="flex-1"><p className="font-semibold">{f.name}</p><p className="text-sm text-muted">{f.category}</p></div><b className="text-ember">${f.price}</b></div>)}</div></section><section className="rounded-[36px] border border-ember/25 bg-ember-gradient p-8 text-center"><Sparkles className="mx-auto"/><h2 className="mt-3 text-3xl font-bold">Unlock members-only premieres</h2><p className="mx-auto mt-2 max-w-xl text-white/80">Subscribe for reminders, offers, loyalty food bundles, and early seat access.</p><div className="mx-auto mt-5 flex max-w-md gap-2 rounded-2xl bg-black/40 p-2"><input className="min-w-0 flex-1 bg-transparent px-3 outline-none" placeholder="you@example.com"/><button className="rounded-xl bg-black px-4 py-3"><ArrowRight/></button></div></section></div>}
function Section({title,action,children}:{title:string;action:string;children: ReactNode}){return <section><div className="mb-5 flex items-center justify-between"><h2 className="text-2xl font-bold md:text-3xl">{title}</h2><button className="text-sm text-ember">{action}</button></div>{children}</section>}
